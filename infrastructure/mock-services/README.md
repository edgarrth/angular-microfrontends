# Mock services

The mock REST services are implemented as independent Node/Express backends in `backends/`.
They use JSON datasets from `datasets/json` and do not require cloud services, identity providers or databases.
